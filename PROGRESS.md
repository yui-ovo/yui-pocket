# 进度

## 2026-09-14 · GitHub 扩展发行 0.1.1（已发布）

- 用户修订原“仅 JSON”交付要求，明确授权公开仓库 yui-ovo/yui-pocket，包含当前供图。已通过 GitHub 页面在该账号下创建仓库；未上传真实聊天、用户预设、私人配置或本地临时文件。
- 新增根目录 manifest.json、构建 extension.js、src/extension.ts。扩展当前不依赖助手，共用 phone/demo/host 源码；Helper JSON 仍保留。扩展发行号 0.1.1 表示新包装，功能仍为 v0.1 演示，没有推进阶段 2。
- 核实官方 SillyTavern 1.18.0 固定 commit 的模块加载、activate/enable/disable/delete 钩子、Git clone/pull 和刷新应用更新，见 SOURCES。挂载目标显式区分当前扩展窗口和助手父窗口；无网络更新器、无持久设置或新增运行依赖。
- 最终 npm test：类型检查、双格式构建、4/4 产物测试、16/16 浏览器模拟测试通过（13.9秒）。首次扩展监听检查误把 Playwright 的监听器纳入；修改测试为按扩展调用栈追踪后完整重跑通过。没有放宽实际清理要求。模块启停/删除/重复调用/替换、旧 Helper 卸载、延迟 ready 和 pagehide、无主动网络/存储、原UI测试通过。
- 更新 AGENTS、ROADMAP、README，用户安装入口改为扩展仓库；安装前停用原脚本，更新后刷新。源码、manifest 和构建文件一起发布，发布校验结果在后续条目记录。
- 首次文件树提交因包含大体积备用 JSON 而超过自动审查的200000字节上限，被自动审批拒绝。缩小发布范围为扩展构建和源码，不再在仓库重复存放整份 JSON；本地 JSON 交付及生成能力保留。
- 已发布至 https://github.com/yui-ovo/yui-pocket 的 main，功能提交 `7a26e1ae5614f3599ca3fa8389b87983fcaa1453`。重新下载该提交的 manifest.json 和 extension.js，清单入口/版本正确，扩展 SHA-256 与本地一致：`dc872f28fb2ce528b7667e136909db077efebde179f1b2d4a612722be0968bb2`。当前工程已关联 origin，保留工作文件；无真实聊天或私密配置进入仓库。随后仅补充此发布记录。

待验证：真实 SillyTavern/TauriTavern 从链接安装、更新、禁用，以及手机软键盘。本轮没有登录或操作用户酒馆。无钩子的旧版/分支宿主禁用后须刷新；不能据模拟结果声称真实兼容。当前演示限制和供图来源说明不变。

## 2026-09-14 · 透明挂绳、缩短白边与 Home 居中（已完成）

- 先读取长期规则、路线和进度，仅完成用户要求的 v0.1 外观修改；没有开始阶段 2。
- 两次图像编辑工具返回不透明棋盘格，经检查未采用。用户明确授权本地抠图后，直接处理原图，去除连通白底、细化边缘透明度，保留原始粉色颗粒笔触、珠子和角色的白色填充；不是重新生成的挂绳。完整透明 PNG 为 outputs/yui-strap-cutout.png（593×1368），运行内嵌副本为 303×700。已检查 RGBA 透明通道及深色合成效果。
- 普通机身高宽比从 1.96 改为 1.75；上白边由宽度的 .30 改为 .19，下白边由 .27 改为 .17，合计缩短约 37%，保留主要屏幕高度。低可视高度继续紧凑布局。
- Home 改为显式按屏幕中心定位，普通/紧凑模式均适用；移除右上胶布与右下十字，保留下方左侧光环。透明挂绳替换旧 SVG，不拦截点击。主屏→联系人→聊天导航及演示行为保持。
- 最终运行 npm test：TypeScript、构建、3/3 产物测试和 12/12 模拟浏览器测试通过（12.8 秒）。新增挂绳透明像素与不透明主体检查、五种尺寸 Home 居中误差 <1px、两处旧贴纸不存在、普通白边与机身比例断言；重复初始化、20次开关、清理、文本注入、零主动网络/Storage 调用等原检查继续通过。
- 已目视检查 375×667 聊天/主屏及 375×340 紧凑聊天截图，输入与发送保持可见。截图为本地模拟宿主，不是本轮真机验证。用户提供的 TauriTavern 截图仅用于定位问题。
- 更新 ROADMAP、README、SOURCES 与本进度，整理 outputs/yui.v0.1.json、源码目录和 ZIP。JSON 实际文件为 479915 bytes；SHA-256 为 90434b36745f0f63a6739e2e8a63ba807725c793879337548d9abc665d36969f。

待验证：真实 SillyTavern/TauriTavern 重新导入、移动软键盘开关和缩放、特殊主题。当前仍是无 AI、无存档、无真实正文操作的演示。抠图保留原图柔糊边缘，深色背景下可能仍见少量浅色纤维边；挂绳在窄屏按比例缩小。无已知构建或模拟测试失败。完成后停止，不推进下一阶段。

## 2026-09-14 · Yui 命名、蕾丝贴纸、Dock 和联系人导航（已完成）

- 本轮先读三份长期文件；仅按用户要求修订 v0.1 演示。没有开始阶段 2，没有读取真实联系人/聊天或接入生成。
- 用户可见名称、脚本名、主屏/状态栏和入口均改成 Yui。新构建为 dist/yui.v0.1.json，交付为 outputs/yui.v0.1.json；旧命名产物保留作历史，README 提醒停用旧条目。私有单实例 Symbol 沿用旧值以接替旧实例，其余挂载标识使用新名。
- 用用户给的两张蕾丝 PNG 替换机身平滑矢量贴纸，原文件内嵌，不进行生成或图像重绘；以 CSS 裁显胶布、光环、十字蝴蝶结。原有挂件仍用 SVG。SOURCES 已明确记录供图归属及使用方法。
- 移除底部文案条；采用全宽半透明玻璃分层 Dock，信息图标位于 Dock。只有一个实际应用，没有增加音乐等附加应用。
- 导航为主屏 → Dock 信息 → 联系人列表 → 小桃聊天。聊天返回列表、列表返回主屏、Home 总回主屏。联系人摘要读取同一演示消息集合，不复制聊天数组；返回时刷新摘要。页面切换保留草稿，关闭规则保持原样。
- 运行 `npm test`：TypeScript、构建、3/3 产物检查、12/12 模拟浏览器测试通过（12.6 秒）。扩展三层导航、Dock、供图成功解码、无旧名可见文案、最新消息摘要及10次页面往返检查；原有20次开关、重复初始化/iframe卸载、危险文本、0网络/存储调用、5种尺寸与可视区域测试仍通过。
- 人工检查新版主屏、联系人、聊天截图，包括375px、320px与340px短视口；outputs/preview-contacts-*.png 新增联系人预览。构建约509KB，比上一版增加的体积主要来自内嵌PNG，运行时没有额外依赖或远程素材请求。
- 更新 AGENTS、ROADMAP、README、SOURCES 与本进度；整理 outputs/yui-source/ 和 yui.v0.1-source.zip。标准构建与交付副本按 SHA256 校验，当前校验值见 outputs/SHA256SUMS.txt。

仍待验证：真实 SillyTavern/TauriTavern 导入、移动设备软键盘和主题组合。本轮测试是模拟环境。当前仍是一个虚构联系人、无AI/存档/正文气泡；不宣称完整 iOS 4S 系统复刻。原图白背景使用混合显示，特殊宿主滤镜效果未实测。完成本轮修订后停止。

## 2026-09-14 · v0.1 视觉修订：4S 机身 + 主屏幕（已完成）

- 本轮先读取 AGENTS.md、ROADMAP.md、PROGRESS.md；只处理用户新增的外观与导航要求，不推进阶段 2，不接入正文/AI/存档。
- 将机身改成白色 4S 参考结构：银色侧框、上方感应器/镜头/听筒、宽白边、矩形屏幕、底部圆形 Home 键。正常视口按机身比例缩放，低高度保留紧凑模式保障输入。
- 新增主屏幕；图三爱心信封原图复制到 src/assets/message-icon.jpg，内嵌在 JSON 中，点击进入信息。Home 与返回按钮回主屏幕；× / Escape 关闭。页面之间切换保留临时消息与草稿，关闭清除草稿但保留本次已发演示消息。
- 新增原创 SVG 粉色蝴蝶结、珠链、爱心/星星挂件、爱心翅膀贴饰和主屏幕格纹；信息页使用早期 iOS 方向的渐变标题栏/气泡。未裁取其他参考中的品牌、角色或签名。供图图标的来源与未另行核实许可状态已记录 SOURCES.md。
- 导入结构、适配层及演示数据边界保持原有实现，未新增依赖。更新递归交付收集，源码包包含嵌套 assets/；校验仅允许 SVG namespace、本地渐变和内嵌图片，不引入外部网络资源。
- 最终执行 `npm test`：TypeScript、构建成功，3/3 产物检查、12/12 模拟宿主浏览器测试通过（11.2 秒）。新增覆盖首次主屏幕、内嵌图标解码、10 次页面往返、Home/返回、草稿/消息保留、DOM id 唯一。原有重复初始化、20 次开关、pagehide 清理、注入文本、0 网络/存储调用及 5 种尺寸/可视区域测试继续通过。
- 人工检查主屏幕与信息页的桌面、375px、320px、340px 短视口截图；outputs/preview-home-*.png 为新主屏预览，outputs/preview-*.png 为更新的信息页预览。
- 构建路径仍为 dist/ruru-pocket.v0.1.json（44572 bytes），交付副本为 outputs/ruru-pocket.v0.1.json；新版名称为“ruru-pocket · v0.1 演示 · 4S 外观”，导入前先停用旧条目。当前散列以 outputs/SHA256SUMS.txt 为准，下方 18930 bytes 散列属于上一版历史记录。
- README、ROADMAP、SOURCES 已同步当前导航/素材和测试说明，完成后整理同路径源码 ZIP 并停止。

待验证与限制：仍未在真实 SillyTavern、TauriTavern 或移动真机导入运行；不宣称 1:1 系统复刻。短视口压缩白边；只有信息应用，没有锁屏或其他应用。固定入口、演示不绑定存档、无持久化/AI/正文气泡这些阶段边界不变。状态栏时钟仅显示打开时刻，电量图形是装饰。用户供图图标若以后公开发布，仍需确认来源许可或替换。

## 2026-09-14 · 阶段 0 + 阶段 1（已完成并停止）

- 当前目录检查：仅有空的 work/ 与 outputs/，没有需要覆盖的用户源码；未发现上级 AGENTS.md。
- 已读取用户核心与视觉要求，建立 AGENTS.md 和完整 ROADMAP.md；阶段 2 及以后仅规划，不实现。
- 已核实助手 4.9.5，commit 04a62b763513744b8cb3d85322f9b5c73bdf8a85 的格式、导入逻辑、隐藏 iframe 和 pagehide 清理约定。SillyTavern release 1.18.0，commit 8172dcd0ee672d3cd9a5e5f7af134f91a45cd2b8，仅版本基线。
- 已建立 README.md、SOURCES.md、TypeScript/原生 DOM 工程、esbuild 打包和最小测试配置。顶层开发依赖 3 个，运行时依赖 0 个。
- 完成右侧小入口、打开/关闭、虚构联系人和三个预置示例、输入发送用户气泡；始终显示演示提示，无模型回复、不读真实正文、不存档。
- 奶白浅粉轻复古机身，独立 CSS 花贴饰与星星挂件；没有把用户参考图素材纳入产物。Shadow DOM 隔离，面板关闭即移除面板及其监听器，禁用卸载全部自身节点/监听；无轮询、计时器、观察器。
- 同源父窗口访问和生命周期集中在 host.ts。pagehide 在就绪回调前登记；单实例替换会销毁旧实例，旧 iframe 迟到卸载不会删除新实例。可视区域事件驱动布局。
- 规范化存档身份、正文原位渲染与模型承接都只写入路线图，没有开始阶段 2。

### 实际运行检查

环境：Windows / Node 24.18.0 / npm 11.16.0 / Chrome 153.0.8010.37（headless）/ Playwright 1.63.0。

1. `npm install --save-dev --save-exact typescript esbuild @playwright/test` 成功，lockfile 已生成；npm 当时报 0 vulnerabilities。
2. `npm run typecheck` 成功。首次沙箱构建启动 esbuild 子进程遇到 EPERM；在允许子进程的执行环境重跑后构建成功，没有修改用户全局配置。
3. 最终 `npm test` 成功：类型检查通过，生成 `dist/ruru-pocket.v0.1.json`；3/3 Node 产物检查、11/11 Playwright 模拟宿主检查通过（7.4 秒）。
4. 产物：18930 bytes，JSON 格式及必要字段通过，JS 语法通过，构建图只有 src/ 本地文件，无运行 import。SHA-256：`eb888a323b9fab4ec52e435217f8033629aeb29c58b65c0dcba52fd16282d78d`。
5. 行为：发送/空白拒绝/空重复提交、20 次开关、同 iframe 重复执行、跨 iframe 重载、旧 iframe 卸载、禁用后的迟到就绪回调均通过。关面板移除面板监听；删除 iframe 后根节点和自身登记监听器归零；重新启用恢复三个示例。
6. 安全边界：危险 HTML 作为文本显示，不生成 img/script 元素；测试宿主原文保持一致，按钮和输入可用。对构建脚本执行拦截后 0 网络请求、0 Storage/禁用网络 API 调用。助手自己注入的 CDN 库未在模拟器加载，不能把此结果推广成整个宿主无网络。
7. 5 个尺寸（1280×900、375×667、320×568、375×340、667×375）布局与长文本通过；另测布局高度 800、visualViewport 高度 340/偏移 180。发现仅 CSS max-height 不能覆盖这种键盘场景，已改成真实可视高度控制紧凑样式，最终测试通过。强制宿主 button/textarea 样式未污染手机。
8. 人工查看桌面、窄屏、小屏、横屏、缩小高度、可视区域偏移截图。保存于 outputs/preview-*.png，均明确属于模拟宿主。未连接真实聊天/用户配置。

### 交付

- 标准构建路径：dist/ruru-pocket.v0.1.json；outputs/ruru-pocket.v0.1.json 为相同文件的交付副本。
- outputs/ruru-pocket-source/：经过文件白名单整理的源码、配置、测试、文档和 dist；不含 node_modules、真实数据或 work/official。
- 已执行 `npm run deliver`，确认 dist 与交付 JSON 的 SHA-256 一致；生成 outputs/ruru-pocket.v0.1-source.zip 并检查归档的 25 个文件条目。
- README.md 包含构建、导入、使用、测试、禁用和真机验证步骤；SOURCES.md 有准确平台 commit、依赖版本与实际参考范围。

### 待真实验证与已知限制

- **未在真实 SillyTavern / TauriTavern 导入。** 格式核实、JSON 通过、模拟行为通过是三个独立结论，不代表实际宿主已成功。
- 未在移动真机、iOS/Android 软键盘、Tauri WebView、各类主题/其他扩展组合中验证。iframe 同源和 pagehide 行为需要目标环境确认。
- 入口暂固定不可拖拽；可能与特殊侧栏位置冲突。小于 340px 的可视高度、特殊缩放/安全区尚未保证。
- 单联系人，输入上限 2000 UTF-16 码元，只点按钮发送。关面板保留本次临时数据，重新加载/禁用清空；没有存档绑定，切聊天仍可能显示同一次演示。持久化和分支隔离未实现。
- 无正文气泡、AI、剧情上下文注入，均按本轮范围暂不实现。v0.3 正文原位气泡已明确作为强制核心验收，不是美化选项。
- 上游助手自身有 CDN 注入；ruru-pocket 自包含和零主动网络调用不保证整个助手离线。

本轮完成阶段 0 + 阶段 1 后停止。下一轮先读三个长期文件，等待用户明确指定阶段再继续。

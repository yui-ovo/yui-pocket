# Yui 产品与阶段

## 核心体验与边界

2026-09-14 用户修订交付要求：优先 GitHub 公开仓库 yui-ovo/yui-pocket，经酒馆“安装扩展”安装，后续使用宿主扩展更新再刷新。增加轻量扩展入口和 manifest，保留同源码构建的助手 JSON 备用；扩展发行号 0.1.1，产品仍是 v0.1 演示阶段。此项仅改变安装/生命周期，不授权阶段 2 或模型/存档功能。

这是剧情中可拿起聊天的原创轻量小手机。最终必须分别演示和验证：**正文叙事 → 原位手机消息气泡 → 继续正文叙事**，以及手机里对应的同一条记录。正文气泡不是可选美化，不能统一挪到末尾，也不能只显示在悬浮面板。

正文实际发送/收到的消息进入规范化数据；两个视图读同一来源；点击正文气泡打开对应联系人并定位同一记录。手机中继续聊天须保存，后续正文生成须正确感知，不能形成与主剧情分离的平行聊天。不同存档与分支隔离。

暂定手机发送后角色只在手机回复，回到酒馆生成正文时再承接；用可替换的生成/上下文策略承载，不写死交互选择。第一版无朋友圈、微博、购物、钱包、日记、TTS、生图等应用。

## 视觉方向

2026-09-15追加：扩展0.1.3仅调整美化控制。四角贴纸增加沿屏幕方向的独立左右/上下偏移，各±40px；大小从60–130%改成10–100%，让贴纸可明显缩小并限制过度放大。挂绳保持独立大小控制，旋转从±4°扩大到±45°。旧外观数据补零偏移、过大贴纸收回新上限；没有实现聊天数据阶段。

2026-09-14追加：扩展0.1.2新增主屏“美化”。挂绳在独立前景层，侧边模拟壳体挂孔，避开音量键；仅打开时短暂落定摆动，支持减少动态效果。四角贴纸独立选择、大小/旋转/隐藏；挂绳可替换本地图片、调整大小、轻微角度和左右挂点。用户三张供图整理为36枚透明栅格贴纸，保留默认下方光环。外观仅手动保存到当前浏览器自己的 localStorage 键，恢复默认只清除此键；这是用户新增的美化要求，不是聊天持久化或阶段2。后文旧视觉记录为历史基线。

奶白与浅粉、少女感、轻复古。以用户提供的白色 iPhone 4S 外形为布局参考：银色侧边、矩形屏幕、前置镜头/听筒/感应器、实体圆形 Home 键。按用户移动端反馈，上下白边缩短约37%，普通机身高宽比由1.96改为1.75，保留主要屏幕高度；短视口继续使用紧凑模式。Home以屏幕中心定位；右上与右下贴纸移除，保留下方左侧光环。不宣称逐像素复刻系统。

当前产品名 **Yui**。主屏底部使用用户图三方向的半透明、带横向玻璃分层的应用 Dock，移除原先文案条；只放实际可用的信息图标，不添加虚假的音乐等应用。点击信息先进入联系人列表，选择小桃再进入聊天；聊天返回列表，列表返回主屏，Home 总回主屏，× 关闭。

目前保留用户供图的爱心光环贴饰；右上胶布与右下十字贴纸已移除。挂绳改用本轮用户指定的粉色珠链原图，经用户授权本地抠图，保留柔糊颗粒质感并内嵌透明 PNG；不声称供图由本项目原创。信封继续使用之前用户指定图标。未新增锁屏、滑动解锁、额外应用或真实联系人数据。日夜模式、自定义壁纸以后另行安排。

## 阶段 0：最小工程（本轮）

检查并保留现有文件；建立长期规则、路线、进度、说明、TypeScript + 原生 DOM + 轻量构建和测试。核实助手 JSON 格式、隐藏 iframe 环境与 pagehide 生命周期，记录来源。

## 阶段 1：v0.1 可导入演示（本轮）

自包含 dist/yui.v0.1.json；轻量入口、明确关闭、联系人/头像占位/气泡/输入/发送；少量明确标注的虚构示例。输入仅在当前演示内新增用户气泡，无假 AI 回复，持续显示“演示模式：不调用 AI，不保存聊天”。关面板保留本次内存演示，重新加载/禁用销毁。

隔离样式，适配桌面、窄屏与可视区域变化。验证重复初始化、关闭、pagehide、输入注入与本项目零主动网络请求。禁止 AI、存档读写、正文扫描/修改、用户预设修改、远程运行依赖。模拟宿主与真实宿主测试严格区分。完成后停止。

## 阶段 2：v0.2 数据边界与存档身份（待用户授权）

核实平台的存档/分支/重生成标识及读写事件，先制定规范化消息与持久化策略，再在虚构测试存档中验证。不以角色名字或楼层序号当永久身份。

建议待验证模型：schemaVersion + archiveId + branchId；源记录使用 sourceMessageId + revisionId + 消息在原文的稳定锚点/局部序号；规范消息独立 messageId、conversationId、senderId、recipientId、content 与 origin。来源映射保存原文跨度和版本指纹，楼层序号只作临时定位。平台没有稳定 ID 时，须设计并验证安全的旁路映射与迁移，不能假定已有某 API。切档清空旧视图与旧操作上下文；共享规范仓库提供投影视图，不复制两套聊天数组。

## 阶段 3：v0.3 正文原位气泡（必须，待用户授权）

1. 独立设计简短手机消息格式，明确发送者、接收者、内容，按需包含时间。交付少量手工测试原文及简短 AI 输出规则：只有实际发送/收到的手机消息可用该格式；想法、草稿、旁白不使用。先手动验证，无须调用 AI。
2. 从原始聊天文本解析；在原来的正文位置渲染气泡，不永久改写原文成 HTML，不把渲染后 HTML 当数据源。
3. 正文与手机读同一规范消息，点击气泡打开对应会话并定位同一 messageId；重复渲染、刷新和重处理幂等，修改/删除同步失效，切重生成版本显示正确记录。
4. 优先评估脚本原位渲染的可靠性；确有必要才独立编写配套显示正则。若采用，交付可导入正则 JSON 并说明用途、顺序、作用范围、显示选项和禁用步骤。不得覆盖用户已有正则，不为模仿其他作品增加正则。
5. 验收：单条、相邻多条、中间有叙事、修改、删除、切重生成版本；气泡不吞前后叙事，畸形/不完整格式保留可读原文，不能静默丢内容。不同存档/分支无串记录。

## 阶段 4：手机模型对话与剧情承接（待用户授权）

核实助手生成接口，复用已有模型连接。请求绑定存档/分支快照、操作 ID 与可取消生命周期。用户发送、生成成功、失败、重试和去重分别建模；生成中切档不允许写入新档。

保存手机对话与向后续正文注入上下文分别验收。可替换策略控制“仅手机回复，后续正文承接”；不得把已保存视为已被模型感知。控制上下文长度并保持来源可追踪。

## 阶段 5：集成验收与兼容（待用户授权）

在真实 SillyTavern 和 TauriTavern、桌面与移动设备测试，记录准确版本。覆盖正文原位气泡和同一手机记录、持续剧情、刷新、编辑删除、重生成、分支、切档、异常恢复及卸载。无实际环境就标记待验证，不能用模拟通过代替兼容结论。

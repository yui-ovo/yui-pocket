# Yui 长期规则

- 每轮先读本文件、ROADMAP.md、PROGRESS.md；简述范围，只做用户指定阶段。小步修改，保留用户文件。
- 用户于2026-09-14改为优先从酒馆扩展入口安装/更新：公开仓库 https://github.com/yui-ovo/yui-pocket，根目录 manifest.json + extension.js。同时保留 Tavern Helper 自包含脚本 JSON；两种入口共用源码，不成为独立网站，运行时少依赖。发布需先测试/构建，并提交构建文件；不上传 work、私人配置或真实聊天。
- 独立设计界面、协议和业务逻辑；不下载、复制、改写、执行或远程引入别人的小手机。不以变量改名冒充原创。记录实际来源，不作零相似度保证。
- 接口、导出字段、挂载和生命周期必须核实官方文档、类型或源码，记录版本/commit。未知点明确待验证，不编造 API。
- 平台操作集中于适配层。主目标 SillyTavern，TauriTavern 单列兼容验收；模拟测试不能称为真实导入成功。
- 不改宿主/助手源码、已有主题、扩展、正则或预设。样式隔离；关闭后不挡点击；重复初始化、禁用、卸载须清理节点、监听和计时器。不高频轮询或持续扫描全页。
- 正文原位气泡是核心：原始文本→规范化消息→正文/手机两种视图。HTML 不是数据源，不用两套数组复制同步。
- 存档、分支、来源消息、重生成版本和消息身份分别设计；不能仅以角色名或可变楼层序号标识。
- 模型能力经核实后使用酒馆已有连接；不接触 API 密钥。保存与上下文注入分别实现、验证；失败、重试、重复发送、生成中切档不能重复或错写。
- 输入和模型输出均不可信，只作为文本处理；不上传用户数据，不提交私人配置、真实聊天。测试只用虚构数据。
- 用户已授权美化页：仅把外观选项和用户明确选择的本机图片保存在 yui-pocket.appearance.v1；不保存聊天、不改宿主预设、不上传自定义图片。图片只接受本地栅格格式，校验/限尺寸，关闭后异步结果不得复活旧面板。
- 2026-09-15授权v0.2第一小步：联系人资料、三项开局关系、固定虚构账号与阅读偏好。联系人按核实的宿主账号与聊天定位隔离，人物用独立UUID；本机专用键保存，聊天重命名迁移关联，复制/分支不共享对象。阅读键与美化键分离。只允许核实的本源账号查询、来源卡头像和用户明确指定的HTTP(S)头像图片请求；不写宿主消息/角色卡/世界书/预设，不调用AI。异步操作绑定面板及存档，不得迟到写入。此授权不代表完整v0.2或后续申请/世界书/正文事件已获准。
- 每轮运行相关构建/检查，更新 PROGRESS.md 的实际结果、限制和待验证项，完成指定阶段即停止。
- 2026-09-15 用户追加 TT 适配：TT 使用经核实的 api.chat.open(ref).store，仅操作 yui-pocket 命名空间的联系人资料；不使用 current.handle（内部会读消息数组）、metadata 或底层 invoke。普通 ST 维持本机账号隔离键。TT 原生目录可能按 integrity 共用，Yui 条目必须再按聊天文件名隔离；生命周期/切档校验及无法撤销已发出原生写入的限制须保留。布局只订阅官方 api.layout，不修改宿主界面。
